import { Note as NoteClass } from '../../hooks/usestore';

import Note from './note';

export default function Main({
   val,
   dispatch
}: {
   val: any,
   dispatch: Function;
}) {
   return <main>
   </main>;
}